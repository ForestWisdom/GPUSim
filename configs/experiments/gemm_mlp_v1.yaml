gpu: configs/gpu/a100.yaml
kernel: configs/kernels/cutlass_gemm_tensorop.yaml
model:
  hidden_sizes: [256, 256, 128]
train:
  batch_size: 128
  lr: 0.001
  epochs: 20
dataset:
  train_ratio: 0.8
  val_ratio: 0.1
